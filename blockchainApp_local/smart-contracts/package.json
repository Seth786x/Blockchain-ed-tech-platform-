{
  "name": "edtech-hardware-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for EdTech Hardware Learning Platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:testnet": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "hardhat": "^2.26.2"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
